# ============ 址南针 Agent 核心依赖 ============
# 安装：pip install -r requirements.txt

chainlit>=2.11.0          # 对话式 Web 界面（唯一入口）
langgraph>=0.2.0          # Agent 状态图编排（多阶段决策流）
requests>=2.31.0          # HTTP 请求（高德 API）
python-dotenv>=1.0.0      # .env 配置加载
Pillow>=10.0.0            # 地图/报告图片生成
playwright>=1.40.0        # PDF 报告渲染 + 58同城行情抓取（租金谈判助手依赖）
